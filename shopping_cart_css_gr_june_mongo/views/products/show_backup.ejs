<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show products</title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <%console.log("This is purchaseitems")%>
        <%console.log(purchaseitems)%>
        
        <% for(let purchaseitem of purchaseitems) { %>
            <%console.log(purchaseitem)%>
            <li><a href="/products/<%= product._id %>/purchaseitems/purchaseNew"></a>Quantity is: <b><%= purchaseitem.quantity %></b></li>
            <% let purchaseitemTotal = product.price * purchaseitem.quantity %>
            <p> <%=`Your total purchase price for this is: `%><b>£ <%=`${purchaseitemTotal}` %></b></p>

        <% } %> 
     
    </ul>
   
    <h2>Product price: <%= product.price %></h2>
    
    <a href="/products">Back to Products</a>
    
</body>
</html>